@model NewsDetailsViewModel

@{
	ViewData["Title"] = Model.Title;
}

<a href='javascript:history.go(-1)' class="btn btn-outline-secondary mb-2"><i class="bi bi-arrow-left"></i> Terug</a>

<img src="https://www.tvdordrecht.nl/media/@Model.ImagePath" class="mb-2" style="width: 100%; height: 30vw;object-fit: cover;" />

<h1>@Model.Title</h1>

</p>@Model.DateTime.GetValueOrDefault().ToString("d MMMM yyyy") door @Model.Owner</p>

@Html.Raw(Model.Text)